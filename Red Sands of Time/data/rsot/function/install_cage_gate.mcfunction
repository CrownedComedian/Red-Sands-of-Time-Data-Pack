# Executed only once ever

# Init cage gate data
$data modify storage rsot:game cage.x set value $(x)
$data modify storage rsot:game cage.y set value $(y)
$data modify storage rsot:game cage.z set value $(z)

setblock ~ ~ ~ minecraft:bedrock